(()=>{
  const A=window.DockerCourse;
  if(!A)return;
  A.addDeck({"id":"docker-04-compose","badge":"Docker 04","title":"Docker — 04. Docker Compose","subtitle":"35 cartes pour décrire et piloter une application multi-conteneurs.","description":"Services, projets, réseaux, volumes, secrets, healthchecks, dépendances, commandes et environnements.","phrases":["Compose décrit un modèle d’application, pas seulement une suite de commandes.","Démarré ne signifie pas prêt : utilise healthcheck et retry.","`down` garde les volumes par défaut ; `down -v` peut détruire les données.","Vérifie le modèle effectif avec `docker compose config`."],"cards":[["Sources","Quelles pages officielles garder pour maîtriser Docker Compose ?","Introduction et modèle Compose :\nhttps://docs.docker.com/compose/\nhttps://docs.docker.com/compose/gettingstarted/\nhttps://docs.docker.com/reference/compose-file/\nhttps://docs.docker.com/reference/compose-file/services/\n\nRéseau, démarrage et variables :\nhttps://docs.docker.com/compose/how-tos/networking/\nhttps://docs.docker.com/compose/how-tos/startup-order/\nhttps://docs.docker.com/compose/how-tos/environment-variables/\nhttps://docs.docker.com/reference/compose-file/secrets/"],["Définition","Qu’est-ce que Docker Compose ?","Un outil et une spécification permettant de décrire puis gérer une application multi-conteneurs dans un ou plusieurs fichiers YAML."],["Modèle","Quels objets principaux un fichier Compose peut-il déclarer ?","Des services, réseaux, volumes, secrets et configurations. Les services décrivent les conteneurs à créer."],["Service","Qu’est-ce qu’un service Compose ?","La définition d’un rôle applicatif — web, API, base, worker — à partir d’une image ou d’un build, avec sa commande, son environnement, ses montages, réseaux et politiques."],["Projet","Qu’est-ce qu’un projet Compose ?","Le groupe isolé de ressources créé à partir d’une configuration : conteneurs, réseau par défaut et volumes. Son nom sert de préfixe et permet plusieurs environnements sur un même hôte."],["Fichier","Quel nom de fichier privilégier aujourd’hui ?","`compose.yaml` ou `compose.yml`. L’ancienne clé top-level `version` n’est plus le moyen central de choisir une syntaxe : la Compose Specification fait référence."],["Image / build","Quelle différence entre `image:` et `build:` dans un service ?","`image` indique une image à tirer ou utiliser ; `build` décrit comment la construire. Un service peut combiner les deux pour construire puis taguer le résultat."],["Commande","Que remplacent `command:` et `entrypoint:` ?","`command` remplace le `CMD` de l’image ; `entrypoint` remplace son `ENTRYPOINT`. Les utiliser sans comprendre l’image peut casser son démarrage."],["Ports","Que signifie `ports: - \"8080:80\"` ?","Le port 80 du conteneur est publié sur le port 8080 de l’hôte. Les guillemets évitent certaines interprétations YAML ambiguës."],["Expose","À quoi sert `expose:` dans Compose ?","À documenter ou exposer des ports aux services du réseau sans les publier sur l’hôte. La communication interne utilise le nom du service et le port du conteneur."],["Variables","Quelles méthodes fournissent des variables à un service ?","`environment`, `env_file`, interpolation `${VARIABLE}` depuis l’environnement ou `.env`, et options de la CLI. Il faut connaître la priorité des sources."],[".env","À quoi sert le fichier `.env` de Compose ?","Principalement à fournir des valeurs d’interpolation au modèle Compose. Il ne faut pas le confondre automatiquement avec un `env_file` injecté dans le conteneur."],["Secrets","Pourquoi utiliser `secrets` plutôt que mettre un mot de passe dans `environment` ?","Le secret n’est accordé qu’aux services explicitement autorisés et est présenté comme un fichier. Cela limite l’exposition dans la configuration et l’environnement des processus."],["Volumes","Comment déclarer un volume nommé partagé ?","Le référencer dans `services.<nom>.volumes`, puis le déclarer dans la section top-level `volumes:`. Compose crée généralement un volume préfixé par le nom du projet."],["Réseau par défaut","Que crée Compose si aucun réseau n’est déclaré ?","Un réseau bridge par défaut pour le projet. Chaque service y est joignable par son nom grâce au DNS interne."],["Réseaux multiples","Pourquoi déclarer plusieurs réseaux dans Compose ?","Pour segmenter les communications : par exemple `front` entre proxy et API, `back` entre API et base. Seuls les services partageant un réseau peuvent communiquer."],["depends_on","Que garantit la forme courte de `depends_on` ?","L’ordre de création et d’arrêt, mais pas que la dépendance soit prête à accepter des requêtes. Un processus peut être démarré sans être opérationnel."],["Santé","Comment attendre qu’une base soit réellement prête ?","Définir un `healthcheck` sur la base et utiliser la forme longue de `depends_on` avec `condition: service_healthy`, tout en gardant une logique de retry dans l’application."],["Healthcheck","Quel est le but d’un healthcheck ?","Tester l’aptitude réelle du service à remplir sa fonction, pas seulement l’existence du processus. Le test doit être léger, pertinent et indépendant d’un service externe inutile."],["Restart","Que signifie `restart: unless-stopped` ?","Le conteneur est relancé après une panne ou un redémarrage du daemon, sauf s’il a été arrêté volontairement. Cela ne remplace pas la résolution de la cause d’un crash."],["Up","Que fait `docker compose up -d` ?","Il crée ou met à jour les ressources nécessaires et démarre les services en arrière-plan. Compose peut recréer les conteneurs dont la configuration ou l’image a changé."],["Build","Quand utiliser `docker compose up -d --build` ?","Lorsque des images construites localement doivent être reconstruites avant le démarrage. Sans `--build`, une modification du code copié dans l’image peut ne pas être intégrée."],["Down","Que fait `docker compose down` ?","Il arrête et supprime les conteneurs et réseaux du projet. Les volumes nommés ne sont pas supprimés par défaut."],["Danger","Pourquoi `docker compose down -v` est-il dangereux ?","`-v` supprime les volumes nommés déclarés et les volumes anonymes attachés. Pour une base de données, cela peut détruire les données si aucune sauvegarde valide n’existe."],["Stop / start","Quelle différence entre `stop`, `start`, `restart` et `down` ?","`stop` garde les conteneurs ; `start` les redémarre ; `restart` redémarre les conteneurs existants sans appliquer nécessairement les changements de configuration ; `down` les supprime."],["Recréation","Pourquoi `docker compose restart` ne suffit-il pas après un changement de variable ou de YAML ?","Il redémarre l’ancien conteneur avec son ancienne configuration. Utilise `up -d`, éventuellement `--force-recreate`, pour appliquer le nouveau modèle."],["État","Quelles commandes donnent une vue rapide du projet ?","`docker compose ps`, `docker compose images`, `docker compose top`, `docker compose logs`, `docker compose config` et `docker compose ls`."],["Logs","Comment suivre les logs d’un seul service ?","`docker compose logs -f --tail 200 SERVICE`. Le nom du service est plus stable que celui d’un conteneur généré."],["Exec / run","Quelle différence entre `docker compose exec` et `docker compose run --rm` ?","`exec` lance une commande dans un conteneur déjà actif ; `run` crée un conteneur ponctuel à partir de la définition du service, utile pour migration, test ou tâche administrative."],["Config","Pourquoi exécuter `docker compose config` avant un déploiement ?","Pour produire le modèle fusionné et interpolé, détecter des erreurs et vérifier les valeurs réellement appliquées sans lancer les services."],["Fichiers multiples","À quoi servent plusieurs fichiers Compose avec `-f` ?","À fusionner une base avec des variantes de développement, production ou test. Il faut inspecter le résultat avec `docker compose config` car les règles de fusion diffèrent selon les champs."],["Profiles","À quoi servent les profiles Compose ?","À activer seulement certains services optionnels, par exemple outils de debug ou jobs, sans dupliquer tout le fichier."],["Scaling","Que fait `docker compose up --scale worker=3` ?","Il crée plusieurs instances du service `worker`, si sa définition le permet. Un `container_name` fixe empêche généralement cette mise à l’échelle."],["Atelier","Mission — écrire une stack web + base robuste.","Crée un `compose.yaml` avec :\n- une application ;\n- une base non publiée sur l’hôte ;\n- un volume nommé pour la base ;\n- un réseau backend ;\n- un healthcheck ;\n- `depends_on: condition: service_healthy` ;\n- une politique de redémarrage ;\n- une variable non sensible et un secret séparé.\n\nValide avec `docker compose config`, puis teste `up`, `logs`, `exec`, `stop`, `start` et `down` sans `-v`."],["Validation","Quand peux-tu considérer le niveau 04 comme maîtrisé ?","Quand tu peux lire, écrire et expliquer un modèle Compose, appliquer une modification sans perdre les données, diagnostiquer l’ordre de démarrage et isoler correctement services, secrets, réseaux et volumes."]]});
})();
