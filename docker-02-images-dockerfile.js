(()=>{
  const A=window.DockerCourse;
  if(!A)return;
  A.addDeck({"id":"docker-02-images-dockerfile","badge":"Docker 02","title":"Docker — 02. Images & Dockerfile","subtitle":"36 cartes pour construire des images efficaces, reproductibles et publiables.","description":"Dockerfile, contexte de build, cache, couches, multi-stage, tags, digests, BuildKit et registres.","phrases":["Le contexte de build doit être minimal et maîtrisé.","Un tag est mutable ; un digest identifie un contenu.","Optimiser l’ordre des couches accélère les builds.","Le stage final ne doit contenir que ce qui est nécessaire au runtime."],"cards":[["Sources","Quelles pages officielles garder pour apprendre les images et Dockerfile ?","Construction d’images :\nhttps://docs.docker.com/get-started/docker-concepts/building-images/\nhttps://docs.docker.com/build/concepts/dockerfile/\nhttps://docs.docker.com/build/building/best-practices/\nhttps://docs.docker.com/get-started/docker-concepts/building-images/multi-stage-builds/\nhttps://docs.docker.com/build/cache/\nhttps://docs.docker.com/reference/dockerfile/\n\nRegistres et publication :\nhttps://docs.docker.com/get-started/docker-concepts/the-basics/what-is-a-registry/\nhttps://docs.docker.com/get-started/docker-concepts/building-images/build-tag-and-publish-an-image/"],["Dockerfile","Qu’est-ce qu’un Dockerfile ?","Un fichier texte déclaratif contenant les instructions utilisées par le builder pour produire une image reproductible et versionnable."],["Build","Que signifie le point dans `docker build -t monapp:1.0 .` ?","Le point désigne le contexte de build. Docker peut utiliser les fichiers de ce contexte pour les instructions comme `COPY`; il ne s’agit pas simplement du dossier où se trouve le Dockerfile."],["Contexte","Pourquoi garder le contexte de build petit ?","Un contexte volumineux ralentit l’envoi au builder, augmente les risques de fuite de fichiers et peut invalider inutilement le cache."],[".dockerignore","À quoi sert `.dockerignore` ?","À exclure du contexte les fichiers inutiles ou sensibles : `.git`, dépendances locales, logs, secrets, artefacts de build et gros fichiers."],["FROM","Quel rôle joue `FROM` ?","Il choisit l’image de base et démarre un stage de build. Plusieurs `FROM` permettent un build multi-stage."],["Image de base","Comment choisir une image de base ?","Privilégier une image officielle ou de confiance, adaptée au runtime, maintenue, raisonnablement minimale et compatible avec les bibliothèques nécessaires. Vérifier sa provenance et ses mises à jour."],["Tags","Pourquoi `latest` n’est-il pas une version fiable ?","`latest` est seulement un tag par défaut et peut pointer vers un contenu différent plus tard. Utilise un tag de version explicite, et un digest lorsque la reproductibilité stricte est requise."],["Digest","Que garantit un digest d’image ?","Il identifie un contenu immuable par son hachage. Pinner `image:tag@sha256:...` évite qu’un tag mutable change silencieusement le contenu utilisé."],["RUN","Que fait `RUN` dans un Dockerfile ?","Il exécute une commande pendant le build et enregistre le résultat dans une nouvelle couche. Il ne s’exécute pas à chaque démarrage du conteneur."],["COPY","Que fait `COPY` ?","Il copie des fichiers du contexte ou d’un autre stage dans l’image. Il est généralement préférable à `ADD` lorsque seule une copie est nécessaire."],["ADD","Quand `ADD` peut-il être pertinent ?","Pour ses comportements spécifiques, comme l’extraction automatique d’une archive locale ou certaines sources distantes. Sinon, `COPY` exprime plus clairement l’intention."],["WORKDIR","Pourquoi utiliser `WORKDIR` plutôt que multiplier les `cd` ?","Il fixe un répertoire de travail explicite pour les instructions suivantes et pour le runtime. Il rend le Dockerfile plus lisible et robuste."],["ENV / ARG","Quelle différence entre `ARG` et `ENV` ?","`ARG` existe pendant le build et n’est pas automatiquement disponible au runtime ; `ENV` devient une variable de l’image et du conteneur. Aucun des deux ne doit recevoir un secret de build sensible."],["EXPOSE","Que fait réellement `EXPOSE 8080` ?","Il documente le port prévu par l’application dans l’image. Il ne publie pas ce port sur l’hôte ; `docker run -p` ou Compose doit le faire."],["USER","Pourquoi définir `USER` dans l’image finale ?","Pour exécuter l’application avec un utilisateur non-root lorsque les privilèges ne sont pas nécessaires, ce qui réduit l’impact d’une compromission."],["CMD","Quel rôle joue `CMD` ?","Il fournit la commande ou les arguments par défaut du conteneur. Les arguments placés après le nom de l’image dans `docker run` peuvent généralement le remplacer."],["ENTRYPOINT","Quel rôle joue `ENTRYPOINT` ?","Il définit l’exécutable principal de l’image. Les arguments de `docker run` sont souvent ajoutés à cet exécutable, selon la forme utilisée."],["Forme exec","Pourquoi préférer souvent `CMD [\"app\", \"--serve\"]` à `CMD app --serve` ?","La forme exec ne passe pas par un shell implicite et transmet plus directement les signaux au processus, ce qui améliore les arrêts gracieux et évite des surprises d’expansion."],["ENTRYPOINT + CMD","Comment combiner `ENTRYPOINT` et `CMD` proprement ?","Utiliser `ENTRYPOINT` pour l’exécutable stable et `CMD` pour ses arguments par défaut. L’utilisateur peut alors changer les arguments sans remplacer l’exécutable."],["Couches","Comment les instructions influencent-elles les couches ?","Les instructions qui modifient le système de fichiers produisent des couches. Docker réutilise les couches inchangées entre builds, ce qui rend l’ordre des instructions important."],["Cache","Quelle règle simple améliore le cache d’un Dockerfile ?","Placer d’abord les étapes qui changent rarement, puis les fichiers qui changent souvent. Par exemple copier les manifestes de dépendances, installer, puis copier le code source."],["Paquets","Pourquoi regrouper mise à jour, installation et nettoyage des paquets dans un même `RUN` ?","Pour éviter de conserver des index obsolètes dans une couche précédente et limiter la taille de l’image. La syntaxe exacte dépend de la distribution."],["Multi-stage","Qu’est-ce qu’un build multi-stage ?","Un Dockerfile avec plusieurs stages : l’un compile ou assemble l’application, puis le stage final ne copie que les artefacts nécessaires au runtime."],["Bénéfice multi-stage","Quels sont les principaux bénéfices du multi-stage ?","Image finale plus petite, moins d’outils de compilation et de dépendances inutiles, surface d’attaque réduite et séparation claire build/runtime."],["Secrets de build","Pourquoi ne faut-il pas copier une clé ou passer un secret avec `ARG` ?","Le secret peut rester dans l’historique, le cache ou une couche. Utilise les montages de secrets BuildKit afin qu’il soit disponible temporairement sans être intégré à l’image."],["Cache mount","À quoi sert un cache mount BuildKit ?","À réutiliser un cache de gestionnaire de paquets entre builds sans l’intégrer à l’image finale, ce qui accélère les reconstructions."],["BuildKit","Quel est le rôle de BuildKit ?","C’est le backend moderne de build Docker : parallélisation, meilleur cache, montages temporaires, secrets, sorties multiples, builds multi-plateformes et attestations."],["Tag","Que représente un tag comme `monapp:1.4.2` ?","Un nom humain pointant vers une image. Plusieurs tags peuvent référencer le même contenu ; un tag n’est pas une copie indépendante de toutes les couches."],["Registry","Quelle différence entre registre et dépôt d’images ?","Le registre est le service qui héberge des images ; un repository regroupe les versions d’une image dans ce registre. Docker Hub, GHCR, ECR ou ACR sont des registres."],["Push","Quelle séquence permet de publier une image ?","Construire et taguer avec le nom complet du registre, s’authentifier si nécessaire, puis `docker push registre/namespace/image:tag`. Vérifier aussi le digest publié."],["Historique","À quoi servent `docker image inspect` et `docker image history` ?","`inspect` montre métadonnées et configuration ; `history` montre les couches et commandes de construction, utile pour repérer les couches volumineuses ou des informations indésirables."],["Multi-architecture","Pourquoi une image peut-elle fonctionner sur amd64 mais pas arm64 ?","Les binaires et couches sont liés à une plateforme. Une image multi-architecture publie un manifeste qui pointe vers une variante compatible pour chaque architecture."],["Atelier","Mission — construire une image simple et expliquer son cache.","Crée une petite application ou un fichier HTML, puis :\n1. écris un Dockerfile ;\n2. ajoute un `.dockerignore` ;\n3. construis avec `docker build -t cours-docker:1.0 .` ;\n4. lance et teste l’image ;\n5. modifie seulement le code et reconstruis ;\n6. observe quelles étapes utilisent le cache avec la sortie du build ;\n7. examine `docker image history cours-docker:1.0`."],["Atelier","Mission — transformer le build en multi-stage.","Prends une application nécessitant un outil de compilation ou de bundling :\n1. crée un stage `build` ;\n2. produis les artefacts ;\n3. copie uniquement le résultat dans un stage runtime plus petit ;\n4. compare les tailles, paquets et processus des deux approches ;\n5. exécute l’image finale avec un utilisateur non-root si possible."],["Validation","Quand peux-tu considérer le niveau 02 comme maîtrisé ?","Quand tu sais écrire et expliquer un Dockerfile, optimiser le cache, produire une image multi-stage, la taguer, l’inspecter, la publier et éviter d’y inclure des secrets."]]});
})();
