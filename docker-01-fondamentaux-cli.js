(()=>{
  const A=window.DockerCourse;
  if(!A)return;
  A.addDeck({"id":"docker-01-fondamentaux-cli","badge":"Docker 01","title":"Docker — 01. Fondamentaux & CLI","subtitle":"34 cartes pour comprendre le modèle mental, lancer des conteneurs et observer leur cycle de vie.","description":"Images, conteneurs, daemon, registre, commandes essentielles, processus principal, ports et premiers diagnostics.","phrases":["Une image est immuable ; un conteneur est une instance éphémère.","Observer avant d’agir : ps, logs, inspect, stats.","Les données importantes ne doivent pas dépendre de la couche inscriptible du conteneur.","Le processus principal s’arrête : le conteneur s’arrête."],"cards":[["Sources","Quelles pages officielles garder ouvertes pour débuter avec Docker ?","Parcours d’introduction :\nhttps://docs.docker.com/get-started/\nhttps://docs.docker.com/get-started/docker-overview/\nhttps://docs.docker.com/get-started/docker-concepts/the-basics/what-is-a-container/\nhttps://docs.docker.com/get-started/docker-concepts/the-basics/what-is-an-image/\nhttps://docs.docker.com/reference/cli/docker/\n\nAtelier officiel :\nhttps://docs.docker.com/get-started/workshop/\n\nLes commandes évoluent : utilise `docker COMMAND --help` et la documentation de ta version comme références finales."],["Modèle mental","Docker, c’est quoi en une phrase ?","Une plateforme qui construit des images puis exécute, distribue et administre des processus isolés appelés conteneurs."],["Architecture","Quels sont les trois acteurs de base de Docker ?","Le client Docker reçoit tes commandes ; le daemon ou moteur Docker gère images, conteneurs, réseaux et volumes ; le registre stocke et distribue les images."],["Conteneur","Qu’est-ce qu’un conteneur ?","Un processus isolé avec son propre système de fichiers, son réseau et sa configuration, mais qui partage le noyau de l’hôte au lieu d’embarquer un système d’exploitation complet."],["VM","Quelle différence essentielle entre un conteneur et une machine virtuelle ?","Une VM virtualise une machine et possède son propre noyau ; un conteneur isole des processus qui partagent le noyau de l’hôte. Les conteneurs sont généralement plus légers, sans être des VM miniatures."],["Image","Qu’est-ce qu’une image Docker ?","Un paquet standardisé, immuable et composé de couches, contenant les fichiers, binaires, bibliothèques, métadonnées et configuration nécessaires pour créer un conteneur."],["Image / conteneur","Quelle analogie simple distingue image et conteneur ?","L’image est le moule ou la recette versionnée ; le conteneur est une instance créée à partir de cette image et dotée de son propre état d’exécution."],["Immutabilité","Une image déjà construite est-elle modifiée lorsqu’un conteneur écrit un fichier ?","Non. Le conteneur écrit dans une couche inscriptible propre à cette instance. Pour changer durablement l’image, il faut en construire une nouvelle."],["Éphémère","Pourquoi dit-on qu’un conteneur est éphémère ?","Il doit pouvoir être supprimé puis recréé à partir de son image et de sa configuration. Les données importantes doivent donc vivre hors de sa couche inscriptible, par exemple dans un volume."],["Premier lancement","Que fait `docker run nginx` ?","Il cherche l’image localement, la télécharge si nécessaire, crée un nouveau conteneur, configure son environnement puis lance son processus principal."],["Création / démarrage","Quelle différence entre `docker create`, `docker start` et `docker run` ?","`docker create` crée sans démarrer ; `docker start` démarre un conteneur existant ; `docker run` combine création et démarrage d’un nouveau conteneur."],["Inventaire","Que montrent `docker ps` et `docker ps -a` ?","`docker ps` montre les conteneurs en cours d’exécution ; `docker ps -a` inclut aussi les conteneurs arrêtés."],["Nom / ID","Pourquoi donner un nom explicite avec `--name` ?","Un nom stable est plus lisible qu’un ID aléatoire et facilite `logs`, `exec`, `inspect`, `stop` et les scripts. Les noms de services Compose jouent aussi un rôle DNS."],["Arrière-plan","Que signifie `-d` dans `docker run -d` ?","Detached : le conteneur tourne en arrière-plan et le terminal te rend la main. Il faut ensuite observer son état et ses logs explicitement."],["Interactif","À quoi servent `-i` et `-t` ?","`-i` garde l’entrée standard ouverte ; `-t` alloue un pseudo-terminal. On les combine souvent avec `-it` pour un shell interactif."],["Processus principal","Quel rôle joue le processus principal du conteneur ?","C’est le PID 1 du conteneur. Quand il s’arrête, le conteneur s’arrête. Il doit aussi recevoir les signaux et gérer correctement ses processus enfants."],["CMD / ENTRYPOINT","Que lance Docker au démarrage d’un conteneur ?","La combinaison `ENTRYPOINT` et `CMD` définie dans l’image, éventuellement remplacée ou complétée par les arguments de `docker run`."],["Arrêt","Quelle différence entre `docker stop` et `docker kill` ?","`stop` envoie d’abord un signal d’arrêt gracieux puis force après un délai ; `kill` envoie immédiatement un signal, SIGKILL par défaut. Commence normalement par `stop`."],["Suppression","Que supprime `docker rm` ?","Un conteneur, pas l’image dont il provient ni les volumes nommés. Un conteneur doit normalement être arrêté, sauf usage explicite de `-f`."],["Auto-suppression","À quoi sert `docker run --rm` ?","À supprimer automatiquement le conteneur lorsqu’il s’arrête. C’est pratique pour les commandes ponctuelles et les tests, mais pas pour une instance dont tu veux inspecter l’état après l’arrêt."],["Ports","Que signifie `-p 8080:80` ?","Le port 8080 de l’hôte est publié vers le port 80 du conteneur. L’ordre est toujours `hôte:conteneur`. Publier un port crée une surface accessible à protéger."],["Variables","Comment fournir une variable d’environnement à un conteneur ?","Avec `-e NOM=valeur`, `--env NOM=valeur` ou `--env-file fichier`. Une variable d’environnement n’est pas un mécanisme idéal pour un secret sensible."],["Logs","Que fait `docker logs -f mon-conteneur` ?","Il affiche puis suit les sorties standard et d’erreur du processus principal, selon le pilote de logs. `--tail`, `--since` et `--timestamps` réduisent le bruit."],["Inspection","À quoi sert `docker inspect` ?","À obtenir la configuration et l’état détaillés en JSON : image, commande, variables, montages, réseau, ports, santé, politique de redémarrage et code de sortie."],["Shell","Quelle différence entre `docker exec -it app sh` et `docker attach app` ?","`exec` lance un nouveau processus dans un conteneur déjà actif ; `attach` relie ton terminal au processus principal existant. Pour diagnostiquer, `exec` est généralement plus sûr."],["Copie","À quoi sert `docker cp` ?","À copier un fichier ou dossier entre l’hôte et un conteneur. Ce n’est pas une stratégie normale de déploiement ou de persistance, mais un outil de diagnostic ou de récupération."],["Ressources","Que montrent `docker stats` et `docker top` ?","`stats` affiche en direct CPU, mémoire, réseau et I/O ; `top` liste les processus du conteneur. Ils aident à distinguer panne applicative et saturation de ressources."],["État","Comment lire rapidement la colonne STATUS de `docker ps -a` ?","`Up` signifie en cours ; `Exited (0)` arrêt normal ; un code non nul signale souvent une erreur ; `Restarting` indique une boucle ; `healthy` ou `unhealthy` vient d’un healthcheck."],["Commande de secours","Quelle séquence observer avant toute action destructive ?","`docker ps -a` → `docker logs --tail 200 NOM` → `docker inspect NOM` → `docker stats --no-stream` → vérifier volumes et réseau. Observer avant de supprimer."],["Prune","Pourquoi faut-il se méfier de `docker system prune` ?","Il supprime des objets inutilisés selon ses options. Avec `-a` ou `--volumes`, les conséquences peuvent être importantes. Toujours inventorier avec `docker system df` et sauvegarder les données avant."],["Un service","Pourquoi recommande-t-on généralement une responsabilité principale par conteneur ?","Pour séparer les cycles de vie, les logs, les mises à jour, la mise à l’échelle et les pannes. Un service peut avoir plusieurs processus internes, mais un conteneur ne devrait pas porter toute l’application."],["Atelier","Mission 1 — vérifier que tu sais exécuter et observer un conteneur.","1. `docker run --name web-demo -d -p 8080:80 nginx`\n2. Ouvre http://localhost:8080\n3. `docker ps`\n4. `docker logs web-demo`\n5. `docker inspect web-demo`\n6. `docker exec -it web-demo sh`\n7. `docker stop web-demo`\n8. `docker rm web-demo`\n\nTu maîtrises la mission si tu peux expliquer chaque étape avant de l’exécuter."],["Atelier","Mission 2 — démontrer l’éphémérité d’un conteneur.","Lance un conteneur Alpine, crée un fichier dans sa couche, supprime le conteneur puis recrée-en un depuis la même image.\n\nBut : constater que le nouveau conteneur ne possède pas le fichier, puis expliquer pourquoi un volume est nécessaire pour une donnée persistante."],["Validation","Quand peux-tu considérer le niveau 01 comme maîtrisé ?","Quand tu sais expliquer image, conteneur, daemon et registre ; lancer, nommer, publier, observer, arrêter et supprimer un conteneur ; et diagnostiquer avant d’agir."]]});
})();
